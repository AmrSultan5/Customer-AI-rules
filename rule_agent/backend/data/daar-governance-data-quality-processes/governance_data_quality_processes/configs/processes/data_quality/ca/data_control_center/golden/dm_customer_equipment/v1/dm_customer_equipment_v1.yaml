transform:
  name: dm_customer_equipment
  operations:
  - kind: read_dataio
    name: read_v_equi
    params:
      dataio:
        ad_id: '{{dataio.ad_id}}'
        layer: curated
        storage_id: '{{dataio.storage_id}}'
      source: sap_s4hana      
      namespace: transactional_data
      object_name: v_equi
      schema_version: v1

  - kind: filter
    name: filter_datbi_spras
    params:
      - condition:
          (datbi = '9999-12-31 00:00:00.0000000' AND spras = 'E' AND isnotnull(kunde) AND kunde <> '')

  - kind: select
    name: select_v_equi
    params:
      columns:
        kunde: customer_code
        equnr: equipment_number
        ansdt: acquisition_date
        eqktx: equipment_description
        typbz: equipment_model
        inbdt: installation_date
        sap_cluster: sap_cluster
      distinct: true
    skip_next: false

  - kind: write_dataio
    name: write_dm_customer_equipment
    params:
      dataio:
        ad_id: "{{dataio.ad_id}}"
        layer: application
        storage_id: "{{dataio.storage_id}}"
      application_name: data_quality
      country: ca
      module: data_control_center
      namespace: golden
      object_name: dm_customer_equipment
      process_run_id: "{{process_run_id}}"
      schema_version: v1
      refresh_mode: full
